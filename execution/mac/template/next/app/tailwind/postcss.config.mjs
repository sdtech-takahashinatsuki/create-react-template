const config = {
    plugins: {
        // For Tailwind v4 the PostCSS plugin is provided by @tailwindcss/postcss
        "@tailwindcss/postcss": {},
        autoprefixer: {}
    }
};

export default config;
